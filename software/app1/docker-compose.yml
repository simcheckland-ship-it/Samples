version: '3.8'

services:
  react-app:
    image: ghcr.io/simcheckland-ship-it/react-app:latest
    container_name: react_frontend
    ports:
      - "3000:3000"  # Maps VM port 3000 to internal container port 3000 (per your Nginx config)
    restart: unless-stopped
